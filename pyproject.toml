[project]
name = "flash-infer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "torch>=2.13.0",
    "triton>=3.7.1",
]
